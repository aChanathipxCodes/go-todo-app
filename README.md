# Installation
## Backend (Go)
$ go mod tidy \
$ go run main.go

## Frontend (React)
$ npm install \
$ npm start

# Technologies Used

Frontend : React, CSS \
Backend : Go (Gin Framework) \
Database : In-memory data storage (for simplicity)
